CONCURRENCY=5
REQUEST_TIMEOUT_MS=15000
USER_AGENT=Mozilla/5.0 (compatible; SEO-Audit-Bot/1.0)
RESPECT_CRAWL_DELAY=true
# Retry on 429/503: number of retries and base delay (doubles each retry)
MAX_RETRIES=3
RETRY_BASE_DELAY_MS=1000
